# Legacy RxFire Tests

These files are preserved as a migration reference for Firebase emulator parity.
They still describe the upstream RxFire Observable behavior, but they are
excluded from the active FireSignals Vitest configs.

New and migrated tests should be Vitest specs using `*.spec.ts` or
`*.emulator.spec.ts`.

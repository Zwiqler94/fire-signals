# Legacy RxFire Tests

These files are preserved as migration reference for Firebase emulator parity.
They still describe the upstream RxFire Observable behavior, but they are not
part of the active FireSignals test suite.

New and migrated tests should be Vitest specs using `*.spec.ts` or
`*.emulator.spec.ts`.

# name: Test and publish
# 
# on:
#   push:
#     branches:
#       - "**"
#     paths-ignore:
#       - "**/*.md"
#   pull_request:
#     branches: 
#       - "**"
#   release:
#     types:
#       - published
# 
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     name: Build
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v7
#       - name: Setup node
#         uses: actions/setup-node@v6
#         with:
#           node-version: '22'
#           cache: npm
#           cache-dependency-path: package-lock.json
#       - name: Install deps
#         run: npm ci
#       - name: Build
#         id: npm-pack-dir
#         run: ./build.sh
#       - name: 'Upload Artifact'
#         uses: actions/upload-artifact@v7
#         with:
#           name: fire-signals-${{ github.run_id }}
#           path: dist
#           retention-days: 1
# 
#   test:
#     runs-on: ubuntu-latest
#     needs: build
#     strategy:
#       matrix:
#         node: ["22", "24"]
#         rxjs: ["6", "7"]
#       fail-fast: false
#     name: Test rxjs@${{ matrix.rxjs }} on Node.js ${{ matrix.node }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v7
#       - name: Setup node
#         uses: actions/setup-node@v6
#         with:
#           node-version: ${{ matrix.node }}
#           check-latest: true
#           cache: npm
#           cache-dependency-path: package-lock.json
#       - name: test/functions node_modules cache
#         id: functions_node_modules_cache
#         uses: actions/cache@v6
#         with:
#           path: ./test/functions/node_modules
#           key: ${{ runner.os }}-${{ matrix.node }}-functions_node_modules-${{ hashFiles('test/functions/package.json') }}
#           restore-keys: |
#             ${{ runner.os }}-${{ matrix.node }}-functions_node_modules-
#       - name: Install deps
#         run: |
#           npm ci
#           npm install --no-save --no-audit --no-fund --package-lock=false rxjs@${{ matrix.rxjs }}
#       - name: Install test/functions deps
#         if: steps.functions_node_modules_cache.outputs.cache-hit != 'true'
#         run: |
#           cd test/functions
#           npm install --no-audit --no-fund
#       - name: Firebase emulator cache
#         uses: actions/cache@v6
#         with:
#           path: ~/.cache/firebase/emulators
#           key: firebase_emulators
#       - name: 'Download Artifacts'
#         uses: actions/download-artifact@v8
#       - name: Run tests
#         run: |
#           mv fire-signals-${{ github.run_id }} dist
#           npm test
#   
#   lint:
#     runs-on: ubuntu-latest
#     name: Lint
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v7
#       - name: Setup node
#         uses: actions/setup-node@v6
#         with:
#           node-version: "22"
#           cache: npm
#           cache-dependency-path: package-lock.json
#           check-latest: true
#       - name: Install deps
#         run: npm ci
#       - name: Run lint
#         run: |
#           npm run lint
# 
#   publish:
#     runs-on: ubuntu-latest
#     name: Publish (NPM)
#     needs: ['build', 'test']
#     if: ${{ github.ref == 'refs/heads/main' || github.event_name == 'release' }}
#     permissions:
#       contents: read
#     steps:
#       - name: Authenticate to Google Cloud
#         uses: google-github-actions/auth@v3
#         with:
#           credentials_json: "${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_NPM_PKG }}"
#       - name: Set up gcloud Cloud SDK environment
#         uses: google-github-actions/setup-gcloud@v3
#       - name: Setup node
#         uses: actions/setup-node@v6
#         with:
#           node-version: '22'
#       - name: Authenticate to Artifact Registry
#         run: |
#           gcloud config set artifacts/repository angular-libraries
#           gcloud config set artifacts/location us-central1
#           gcloud artifacts print-settings npm --scope=@zwiqler94 >> .npmrc
#           npx google-artifactregistry-auth
#       - name: 'Download Artifacts'
#         uses: actions/download-artifact@v8
#       - name: Publish
#         run: |
#           cp .npmrc ./fire-signals-${{ github.run_id }}/.npmrc
#           cd ./fire-signals-${{ github.run_id }}
#           chmod +x publish.sh
#           ./publish.sh
